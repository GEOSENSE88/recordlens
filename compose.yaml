services:
  recordlens:
    image: ghcr.io/geosense88/recordlens:latest
    container_name: recordlens
    restart: unless-stopped
    ports:
      - "${APP_PORT:-3000}:3000"
    environment:
      NODE_ENV: production
      HOST: 0.0.0.0
      PORT: 3000
